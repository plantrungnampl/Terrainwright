interface:
  display_name: "Terrainwright"
  short_description: "Repo-specific patterns and workflows for Terrainwright"
  default_prompt: "Use the Terrainwright repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true